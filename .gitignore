# Dependencias
node_modules

# Salida de build (Vite)
dist
dist-ssr
build
*.local

# Variables de entorno (secretos / local)
.env
.env.*
!.env.example

# Logs y archivos temporales de herramientas
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
build_log.txt

# TypeScript / cachés
*.tsbuildinfo

# Tests / cobertura (si añades Jest/Vitest más adelante)
coverage
*.lcov

# Lockfile alternativo (el proyecto usa npm / package-lock.json)
bun.lockb

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cursor (metadatos locales del workspace, si aparecen en la raíz)
.cursor/

# Sistema operativo
.DS_Store
Thumbs.db
Desktop.ini

# Despliegue local (Vercel, Netlify, etc.)
.vercel
.netlify
